# AI Service environment variables
# Copy to .env and adjust as needed

# No required env vars for basic OCR operation
# Tesseract binary path (override if tesseract is not in PATH)
# TESSERACT_CMD=/usr/bin/tesseract
