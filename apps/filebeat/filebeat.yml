filebeat.inputs:
  - type: container
    paths:
      - /var/lib/docker/containers/*/*.log

processors:
  - add_docker_metadata:
      host: "unix:///var/run/docker.sock"

# Phase 7 — Logstash 경유 출력
output.logstash:
  hosts: ["logstash:5044"]

logging.level: info
logging.to_files: false
